{
  "name": "@aur/agents-identity",
  "version": "0.0.1",
  "private": true,
  "description": "Identity agent. Emits author-level signals (sock-puppet score, age-of-account heuristics).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@aur/core": "workspace:*",
    "@aur/schemas": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
